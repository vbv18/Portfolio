PORT=
CLIENT_ORIGIN=

# SMTP credentials — never commit real values.
# Works with any SMTP provider: Gmail (app password), Resend, SendGrid, AWS SES, etc.
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=

# Where contact-form messages are delivered, and what "From" address to send as.
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
